{
  "routes": [
    {
      "method": "GET",
      "path": "/partitions",
      "handler": "partition.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/partitions/count",
      "handler": "partition.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/partitions/:id",
      "handler": "partition.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/partitions",
      "handler": "partition.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/partitions/:id",
      "handler": "partition.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/partitions/:id",
      "handler": "partition.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
